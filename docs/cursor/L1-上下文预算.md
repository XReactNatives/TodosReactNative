# L1 · 上下文预算管理

知识点来源：《Cursor 方法论》方法5、《最佳实践》模型与上下文。
核心：用 `@` 精确喂上下文，别粘大段代码，别让无关文件污染上下文。

## 本项目的 `@` 喂料配方（按任务）

| 任务 | 建议带的上下文 |
|---|---|
| 改状态层 | `@src/state/store/todos/todosSlice.ts @src/state/store/todos/todosThunks.ts @AGENTS.md` |
| 改展示层 | `@src/presentation/features/todos @compontents.md @.cursor/rules/20-presentation-rn.mdc` |
| 加功能纵切 | `@architecture.md` + 触发 `add-feature-slice` 技能（技能自带模板）|
| 排查数据流 | `@architecture.md`（含时序图）+ 相关单文件 |

## 原则

- Rules 只放真正常驻的常识（四层约束、TS 规范）；专业流程交给按需加载的 Skills。
- 连外部实时数据用 MCP（GitHub 等），而非把内容硬塞进 prompt。
- 保持代码库索引干净，控制上下文体积。

检验：上下文里没有与当前任务无关的噪声。
