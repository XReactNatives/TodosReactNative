# 复制为 .env 并填入真实值；.env 不要提交（已在 .gitignore 忽略）

# GitHub MCP（管 PR / issue）。到 GitHub → Settings → Developer settings → PAT 生成
GITHUB_PERSONAL_ACCESS_TOKEN=

# Cursor SDK（automation/ 与 CI 自动修复用）。Dashboard → API Keys 生成；
# 团队自动化建议用 Service Account key（计费归团队）
CURSOR_API_KEY=
