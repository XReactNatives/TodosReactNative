---
description: 状态层（Redux Toolkit）规范；编写或修改 src/state/store 下的 thunk / slice / selector 时遵守
globs: src/state/**
alwaysApply: false
---

# 状态层：Redux Toolkit 规范

## Thunk（`*Thunks.ts`）

- 一律 `createAsyncThunk<Result, Arg, { rejectValue: AppError }>`，泛型写全，禁止用 `any` 占位。
- `try/catch` 里失败走 `handleApiError(error)` 得到 `AppError`，再 `rejectWithValue(appError)`。
- 用户可感知的副作用用 `showSuccessToast` / `showErrorToast`（`src/utils/toast.ts`）。
- 取数优先调 domain（有业务规则时），否则调 service。

```ts
export const xxxAsync = createAsyncThunk<XxxResult, XxxArg, { rejectValue: AppError }>(
  "todos/xxx",
  async (arg, { rejectWithValue }) => {
    try {
      const result = await xxxFromAPI(arg);
      showSuccessToast("...");
      return result;
    } catch (error) {
      const appError = handleApiError(error);
      showErrorToast(`...: ${appError.message}`);
      return rejectWithValue(appError);
    }
  },
);
```

## Slice（`*Slice.ts`）

- 用 Immer 直接改草稿 state；store 保持**归一化**（`todosById` / `ids` / `usersById` / `sectionsExpanded`）。
- 异步结果在 `extraReducers` 用 builder 消费 `pending / fulfilled / rejected`。
- rejected 复用统一处理器（如 `handleListRejectedAction`），落 `AppError`。

## Selector（`*Selectors.ts`）

- 派生/过滤数据一律 `createSelector` 记忆化；带参选择器用 `(state, arg) => arg` 作输入。
- 不在组件里做过滤/分组计算，收敛到 selector。

## 反例

- ❌ 在组件里手搓 `loading`/`error` 或直接 `fetch`。
- ❌ thunk 泛型写 `any`（现存 `any` 视为待收敛为具体 `*Result` 类型）。
